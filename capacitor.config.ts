import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ACTIVITY-5-JOSE',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
